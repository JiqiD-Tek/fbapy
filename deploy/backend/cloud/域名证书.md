# certbot 方式

## 更新证书

    1. certbot.exe certonly --manual --preferred-challenges dns -d "*.jiqid.net"  (手动DNS TXT记录验证、通配符证书)
    2. certbot.exe certonly --manual --preferred-challenges dns -d "*.jiqid.com"  (手动DNS TXT记录验证、通配符证书)

    保存路径：C:\Certbot\live\
