port: 7880                 # 主信令端口 (HTTP / WebSocket)
rtc:
  tcp_port: 7881           # TCP 回退端口（ICE TCP）
  port_range_start: 50000  # WebRTC RTP 端口
  port_range_end: 52000    # WebRTC RTP 端口
  use_external_ip: true    # 使用宿主机 IP

  turn_servers:
    - host: iot.jiqid.com        # Coturn 公网域名或 IP
      port: 5349                 # Coturn TLS 端口
      protocol: tls              # TLS
      username: admin
      credential: Jiqid_iot_yyds

    - host: iot.jiqid.com
      port: 3478                 # Coturn TCP/UDP 端口
      protocol: tcp              # TCP 回退
      username: admin
      credential: Jiqid_iot_yyds

    - host: iot.jiqid.com
      port: 3478                 # Coturn UDP 端口
      protocol: udp              # UDP
      username: admin
      credential: Jiqid_iot_yyds

redis:
  address: 10.240.225.23:6379   # IP + 端口
  password: "Jiqid_iot_yyds"
  db: 2

turn:
  enabled: false

keys:
  APIuyg8Hzi6QMVY: eOzFAqpaDjolBWshD9XXwXMFeCJF8nHAZeunNuoretbD


logging:
  json: false
  level: info

# 房间设置
room:
  empty_timeout: 15  # 空闲超时时间
  max_participants: 2  # 设备  ←→  Agent（音频） ←→  Azure LLM / ASR / TTS
